# Часто задаваемые вопросы (FAQ)

## 🤔 Общие вопросы

### Q: Что такое Pyrogram?
**A:** Pyrogram - это асинхронный Python клиент для Telegram MTProto API. Позволяет взаимодействовать с Telegram как обычный аккаунт.

### Q: Как это отличается от Telegram Bot API?
**A:**
- **Telegram Bot API** - для ботов, ограниченный функционал
- **MTProto (Pyrogram)** - для обычных аккаунтов, полный функционал

### Q: Можно ли использовать бот вместо обычного аккаунта?
**A:** Нет, боты не получают сообщения из каналов.

### Q: Парсер работает без OpenClaw Gateway?
**A:** Да! Парсер теперь полностью независим. Все сообщения сохраняются в локальную SQLite базу.

---

## 🔑 Проблемы с API ключами

### Q: Где получить TELEGRAM_API_ID и TELEGRAM_API_HASH?
**A:**
1. Перейдите на https://my.telegram.org
2. Авторизуйтесь с номером телефона
3. Перейдите в "API development tools"
4. Создайте приложение
5. Скопируйте App api_id и App api_hash

### Q: Что будет, если потерять TELEGRAM_SESSION_NAME файл?
**A:** Потребуется переавторизоваться:
```bash
rm parser_session.session
python main.py
```

---

## 💾 SQLite база данных

### Q: Где находится БД файл?
**A:** По умолчанию в `parser.db` (корень проекта). Путь задаётся в `.env`:
```env
DB_PATH=parser.db
```

### Q: Как увеличить размер БД?
**A:** Парсер автоматически сохраняет все сообщения. БД растёт по мере поступления сообщений.

### Q: Может ли БД файл занять слишком много места?
**A:** Да, при мониторинге множества каналов. Рекомендуется:
- Регулярно архивировать старые сообщения
- Удалять сообщения старше N дней
- Использовать скрипт очистки

```bash
# Удалить сообщения старше 90 дней
python -c "
import sqlite3
conn = sqlite3.connect('parser.db')
conn.execute(\"DELETE FROM messages WHERE created_at < datetime('now', '-90 days')\")
conn.commit()
conn.close()
"
```

### Q: Как сделать резервную копию БД?
**A:**
```bash
# Простая копия
cp parser.db parser.db.backup

# С архивацией
tar -czf parser_backup.tar.gz parser.db
```

### Q: Как импортировать БД из другой машины?
**A:**
```bash
# Скопируйте файл parser.db с другой машины
scp user@remote:/path/to/parser.db ./parser.db

# Парсер будет использовать существующую БД
python main.py
```

### Q: Как пользоваться SQLite из Python?
**A:**
```python
import sqlite3

conn = sqlite3.connect("parser.db")
cursor = conn.cursor()

# Все сообщения из канала
cursor.execute("SELECT * FROM messages WHERE channel_username = '@news'")
messages = cursor.fetchall()

conn.close()
```

### Q: Как просмотреть содержимое БД?
**A:**
```bash
# Консольный клиент
sqlite3 parser.db

# Команды внутри:
.schema                    # Структура таблицы
SELECT COUNT(*) FROM messages;  # Количество
SELECT * FROM messages LIMIT 5; # Последние 5

# Экспорт в CSV
.mode csv
.output export.csv
SELECT * FROM messages;
.quit
```

### Q: Какие индексы есть в БД?
**A:** Индексы создаются автоматически:
- `idx_messages_timestamp` - для быстрого поиска по времени
- `idx_messages_channel` - для быстрого поиска по каналу

```bash
sqlite3 parser.db ".indexes messages"
```

---

## 📨 Проблемы с сообщениями

### Q: Парсер запущен, но сообщения не поступают в БД
**A:** Возможные причины:

1. **Канал не добавлен:**
   ```json
   {
     "channels": ["@channel_name"],
     "enabled": true
   }
   ```

2. **Парсер отключен:**
   - Проверьте `"enabled": true` в `channels.json`

3. **Нет доступа к каналу:**
   - Убедитесь, что аккаунт подписан на канал

4. **Проверьте логи:**
   ```bash
   LOG_LEVEL=DEBUG python main.py | grep -i "message\|channel"
   ```

### Q: Почему я получаю только новые сообщения?
**A:** Pyrogram получает только сообщения после подключения. История не загружается автоматически.

### Q: Как получить исторические сообщения из канала?
**A:** Требуется модификация кода. Примерно так:
```python
async def get_history(app, chat_id):
    async for message in app.get_chat_history(chat_id, limit=100):
        # Обработать сообщение
        await db.insert_message(payload)
```

### Q: Как обработать сообщения с медиа (фото, видео)?
**A:** Парсер сохраняет только текст. Для медиа требуется:
1. Обнаружить медиа в сообщении
2. Сохранить файл локально или на сервер
3. Сохранить URL в БД

```python
# В handlers/channel_handler.py
if message.photo:
    # Скачать фото
    file_path = await message.download()
    # Загрузить на сервер или сохранить локально
```

### Q: Как индексировать текст сообщений для поиска?
**A:** Создайте индекс FTS (Full Text Search):

```bash
sqlite3 parser.db "
CREATE VIRTUAL TABLE messages_fts USING fts5(text, content='messages');
INSERT INTO messages_fts SELECT text FROM messages;
"
```

Теперь поиск будет быстрее:
```bash
sqlite3 parser.db "SELECT * FROM messages_fts WHERE messages_fts MATCH 'bitcoin';"
```

---

## ⚙️ Конфигурация и логирование

### Q: Какой LOG_LEVEL выбрать для продакшена?
**A:** Используйте `INFO`:
```env
LOG_LEVEL=INFO
```

`DEBUG` генерирует много данных, `ERROR` пропускает важную информацию.

### Q: Где хранится файл логов?
**A:** По умолчанию в `logs/parser.log`. Путь задаётся в `.env`:
```env
LOG_FILE=logs/parser.log
```

### Q: Может ли лог файл занять слишком много места?
**A:** Нет, парсер использует `RotatingFileHandler`:
- При достижении 10 MB создаёт новый файл
- Хранит последние 5 файлов
- Максимум ~50 MB на диске

### Q: Как отключить логирование в файл?
**A:** Оставьте `LOG_FILE` пустым:
```env
LOG_FILE=
```

### Q: Как просмотреть только ошибки в логах?
**A:**
```bash
tail -f logs/parser.log | grep ERROR
grep ERROR logs/parser.log
```

---

## 🔄 Управление каналами

### Q: Как добавить много каналов сразу?
**A:** Отредактируйте `channels.json`:
```json
{
  "channels": [
    "@news",
    "@updates",
    "@alerts",
    "@crypto"
  ],
  "enabled": true
}
```

Парсер загружает список при старте.

### Q: Как удалить все каналы?
**A:** Отредактируйте `channels.json`:
```json
{
  "channels": [],
  "enabled": true
}
```

### Q: Что происходит, если я удалю канал из channels.json?
**A:** Новые сообщения из этого канала не будут сохранены. Старые сообщения остаются в БД.

### Q: Как изменить список каналов во время работы парсера?
**A:** Отредактируйте `channels.json` и перезагрузите парсер:
```bash
Ctrl+C
python main.py
```

Или создайте скрипт, который редактирует JSON динамически.

---

## 🔒 Безопасность

### Q: Безопасно ли хранить .env файл на диске?
**A:** Нет, лучше использовать:
- Переменные окружения системы
- Управление секретами (Vault, etc)
- Docker secrets

Для локальной разработки `.env` в `.gitignore` достаточно.

### Q: Что если кто-то получит доступ к parser.db?
**A:** Они смогут:
- Читать все сохранённые сообщения
- Анализировать данные

**Решение:**
```bash
# Ограничить доступ к БД
chmod 600 parser.db

# Или зашифровать файл (требует модификации)
```

### Q: Может ли аккаунт быть заблокирован Telegram?
**A:** Да, если:
- Отправляет слишком много запросов
- Используется для спама
- Нарушает условия Telegram

**Рекомендации:**
- Используйте отдельный аккаунт для парсера
- Не переборщите с частотой запросов
- Следуйте условиям Telegram

---

## 🐛 Отладка и ошибки

### Q: "TELEGRAM_API_ID and TELEGRAM_API_HASH are required"
**A:**
```bash
cat .env | grep TELEGRAM
```

Убедитесь, что значения заполнены.

### Q: "Session expired" при запуске
**A:** Переавторизуйтесь:
```bash
rm parser_session.session
python main.py
```

### Q: "ModuleNotFoundError: No module named 'aiosqlite'"
**A:**
```bash
pip install -r requirements.txt
```

### Q: Парсер зависает и ничего не логирует
**A:** Возможно зависание при подключении:
1. Установите timeout в коде (требует модификации)
2. Используйте systemd service с `Restart=on-failure`
3. Проверьте доступность сетевых ресурсов

### Q: БД файл повреждён
**A:** Восстановите из резервной копии:
```bash
# Если копия есть
cp parser.db.backup parser.db

# Или восстановите из резервной копии
```

Если нет копии - создайте новую БД:
```bash
rm parser.db  # Будет создана новая при запуске
python main.py
```

### Q: Как отладить проблемы с БД?
**A:**
```bash
# Проверить целостность
sqlite3 parser.db "PRAGMA integrity_check;"

# Оптимизировать
sqlite3 parser.db "VACUUM;"

# Просмотреть статистику
sqlite3 parser.db "PRAGMA table_info(messages);"
```

---

## 📈 Производительность

### Q: Как много каналов может обрабатывать парсер?
**A:** Зависит от:
- Частоты сообщений в каналах
- Объема данных
- Характеристик машины

Теоретически - сотни каналов, практически - протестируйте.

### Q: Парсер потребляет много памяти
**A:** Возможные причины:
1. Большой лог файл - проверьте размер
2. Много каналов - оптимизируйте список
3. Утечка памяти - обновите библиотеки

```bash
# Мониторить память
ps aux | grep python

# Проверить размер БД
ls -lh parser.db

# Проверить размер логов
ls -lh logs/
```

### Q: Как оптимизировать БД?
**A:**
```bash
# Удалить старые сообщения
sqlite3 parser.db "DELETE FROM messages WHERE created_at < datetime('now', '-30 days')"

# Оптимизировать
sqlite3 parser.db "VACUUM;"

# Переиндексировать
sqlite3 parser.db "REINDEX;"
```

---

## 🌐 Развёртывание

### Q: Можно ли запустить парсер на Raspberry Pi?
**A:** Да, но с ограничениями:
- Медленнее из-за слабого процессора
- Может потребоваться swap для памяти

### Q: Как запустить в Docker?
**A:**
```bash
docker build -t telegram-parser .
docker run --env-file .env -v $(pwd)/parser.db:/app/parser.db telegram-parser
```

### Q: Как настроить автоперезагрузку?
**A:** Используйте systemd:
```ini
[Service]
Restart=on-failure
RestartSec=10s
```

### Q: Как запустить несколько экземпляров?
**A:** Используйте разные `DB_PATH` и `parser_session.session`:
```bash
# Инстанс 1
TELEGRAM_SESSION_NAME=parser1 python main.py

# Инстанс 2
TELEGRAM_SESSION_NAME=parser2 python main.py
```

Но будет несколько файлов `parser1.db`, `parser2.db`.

---

## 📱 Интеграция с OpenClaw

### Q: Как OpenClaw читает данные из парсера?
**A:** OpenClaw читает БД напрямую через SQL запросы:
```sql
SELECT * FROM messages 
WHERE channel_username = '@news'
ORDER BY timestamp DESC
LIMIT 100
```

### Q: Нужно ли мне запускать OpenClaw Gateway?
**A:** Нет! Парсер теперь работает полностью независимо. OpenClaw просто читает БД файл.

### Q: Как передать path к БД в OpenClaw?
**A:** Зависит от конфигурации OpenClaw. Обычно:
```yaml
# openclaw config
parser:
  db_path: "/path/to/parser.db"
```

### Q: Может ли OpenClaw и парсер одновременно писать в БД?
**A:** Лучше избежать. Парсер пишет, OpenClaw только читает. Если нужна запись - используйте разные таблицы.

---

## 📞 Если ничего не помогает

### Чек-лист перед обращением за помощью:

1. ✅ Прочитал README.md и ARCHITECTURE.md
2. ✅ Проверил логи с `LOG_LEVEL=DEBUG`
3. ✅ Валидировал конфигурацию в `.env`
4. ✅ Убедился в доступности Telegram сети
5. ✅ Переустановил зависимости: `pip install -r requirements.txt --upgrade`
6. ✅ Попробовал переавторизоваться в Telegram
7. ✅ Проверил целостность БД: `sqlite3 parser.db "PRAGMA integrity_check;"`

### Информация для отправки:

- Версия Python: `python --version`
- Логи с DEBUG: `LOG_LEVEL=DEBUG python main.py 2>&1 | head -100`
- Версии зависимостей: `pip list`
- ОС: `uname -a` (Linux) или `systeminfo` (Windows)
- Ошибка (traceback)
- Размер БД: `ls -lh parser.db`

### Полезные ссылки:

- [Pyrogram Issues](https://github.com/pyrogram/pyrogram/issues)
- [Python Docs](https://docs.python.org/)
- [SQLite Docs](https://www.sqlite.org/docs.html)
- [asyncio Docs](https://docs.python.org/3/library/asyncio.html)
