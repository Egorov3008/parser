# Часто задаваемые вопросы (FAQ)

## 🤔 Общие вопросы

### Q: Что такое Pyrogram?
**A:** Pyrogram - это асинхронный Python клиент для Telegram MTProto API. Позволяет взаимодействовать с Telegram как обычный аккаунт (без ботов).

### Q: Как это отличается от Telegram Bot API?
**A:**
- **Telegram Bot API** - для ботов, ограниченный функционал
- **MTProto (Pyrogram)** - для обычных аккаунтов, полный функционал, но требует авторизации

### Q: Можно ли использовать бот вместо обычного аккаунта?
**A:** Нет, боты не получают сообщения из каналов, только из созданных ими групп.

### Q: Является ли это нарушением условий Telegram?
**A:** Это серая зона. Telegram позволяет использовать MTProto для собственных аккаунтов, но не рекомендует высоконагруженные сценарии. Используйте ответственно.

---

## 🔑 Проблемы с API ключами

### Q: Где получить TELEGRAM_API_ID и TELEGRAM_API_HASH?
**A:**
1. Перейдите на https://my.telegram.org
2. Авторизуйтесь с номером телефона
3. Перейдите в "API development tools"
4. Создайте приложение
5. Скопируйте App api_id и App api_hash

### Q: Ошибка "403 Forbidden" при создании приложения
**A:**
- Убедитесь, что вы авторизованы
- Попробуйте использовать VPN, если доступ ограничен в вашей стране
- Дождитесь подтверждения от Telegram (может занять время)

### Q: Можно ли использовать один API ключ для нескольких аккаунтов?
**A:** Да, один API ключ может использоваться для нескольких сессий, но лучше использовать по ключу на сессию.

### Q: Что будет, если потерять TELEGRAM_SESSION_NAME файл?
**A:** Потребуется переавторизоваться. Просто удалите `parser_session.session` и запустите парсер снова.

---

## 🔗 OpenClaw и WebSocket

### Q: Как узнать URL OpenClaw Gateway?
**A:** Это зависит от вашей установки OpenClaw. По умолчанию `ws://localhost:3000`, но может быть другим.

### Q: Где получить OPENCLAW_GATEWAY_TOKEN?
**A:** Обычно генерируется при инициализации OpenClaw Gateway. Проверьте конфигурацию OpenClaw.

### Q: Ошибка "Failed to handshake with OpenClaw Gateway"
**A:** Возможные причины:
- Gateway недоступен на указанном URL
- Неправильный токен
- Несовместимость версий протокола
- Проблемы с сетью/firewall

**Решение:**
```bash
# Проверьте доступность
curl -v ws://localhost:3000

# Проверьте логи OpenClaw Gateway
# Убедитесь, что сервер запущен
```

### Q: Можно ли использовать HTTPS вместо WebSocket?
**A:** Нет, протокол строго WebSocket (ws:// или wss://).

---

## 📨 Проблемы с сообщениями

### Q: Парсер подключен, но сообщения не поступают
**A:** Возможные причины:

1. **Канал не добавлен:**
   ```json
   {
     "type": "req",
     "id": 1,
     "method": "channel.add",
     "params": {"username": "@channel_name"}
   }
   ```

2. **Бот отключен глобально:**
   ```json
   {
     "type": "req",
     "id": 2,
     "method": "bot.enable",
     "params": {}
   }
   ```

3. **Нет доступа к каналу:**
   - Убедитесь, что аккаунт подписан на канал
   - Публичные каналы должны быть доступны
   - Приватные каналы требуют приглашения

4. **Проверьте логи:**
   ```bash
   LOG_LEVEL=DEBUG python main.py | grep -i "message\|channel"
   ```

### Q: Почему сообщения старые?
**A:** Pyrogram получает только новые сообщения после подключения. История не загружается.

### Q: Как получить старые сообщения из канала?
**A:** Используйте метод `get_chat_history` в Pyrogram (требует изменения кода):
```python
async def get_history(app, chat_id):
    async for message in app.get_chat_history(chat_id, limit=100):
        print(message.text)
```

### Q: Что если в сообщении медиа (фото, видео)?
**A:** Парсер отправляет только текст. Чтобы получить медиа, требуется:
1. Сохранить файл локально
2. Загрузить на внешний сервер
3. Отправить URL в payload

Это требует модификации `handlers/channel_handler.py`.

### Q: Как обработать сообщения с форматированием (bold, italic)?
**A:** Pyrogram предоставляет разные способы:
- `message.text` - просто текст
- `message.html` - HTML форматирование
- `message.markdown` - Markdown форматирование

Измените `handlers/channel_handler.py` для нужного формата.

---

## ⚙️ Конфигурация и логирование

### Q: Какой LOG_LEVEL выбрать для продакшена?
**A:** Используйте `INFO`:
```env
LOG_LEVEL=INFO
```

`DEBUG` генерирует много данных, `ERROR` пропускает важную информацию.

### Q: Может ли лог файл занять слишком много места?
**A:** Парсер использует `RotatingFileHandler`, который автоматически:
- Создаёт резервную копию при достижении 10 MB
- Хранит последние 5 файлов
- Удаляет старые логи

Всего может занять максимум ~50 MB на диске.

### Q: Где хранится файл сессии Telegram?
**A:** В текущей директории с именем `{TELEGRAM_SESSION_NAME}.session`:
```bash
ls -la parser_session.session
```

### Q: Нужно ли резервировать файл сессии?
**A:** Да, если вы хотите переиспользовать сессию на других машинах:
```bash
cp parser_session.session parser_session.session.backup
```

### Q: Можно ли использовать одну сессию на двух машинах одновременно?
**A:** Нет, Telegram разрешит только одно подключение за раз. Вторая машина потеряет соединение.

---

## 🔄 Управление каналами

### Q: Как добавить несколько каналов сразу?
**A:** Отправьте несколько команд `channel.add`:
```python
import asyncio
import json
import websockets

async def add_channels(gateway_url, token, channels):
    async with websockets.connect(gateway_url) as ws:
        # Handshake
        nonce = "test_nonce"
        signature = "test_signature"  # Используйте реальную подпись

        await ws.send(json.dumps({
            "type": "connect",
            "nonce": nonce,
            "signature": signature
        }))

        response = await ws.recv()
        print(f"Connected: {response}")

        # Добавить каналы
        for i, channel in enumerate(channels, 1):
            await ws.send(json.dumps({
                "type": "req",
                "id": i,
                "method": "channel.add",
                "params": {"username": channel}
            }))

            response = await ws.recv()
            print(f"Added {channel}: {response}")
```

### Q: Как удалить все каналы?
**A:**
```python
# Получить список каналов
channels = registry.channels.copy()

# Удалить каждый
for channel in channels:
    registry.remove(channel)
```

### Q: Что происходит с очередью сообщений, если я отключу канал?
**A:** Новые сообщения из отключенного канала не отправляются. Старые сообщения потеряны.

### Q: Можно ли перезагрузить список каналов из файла?
**A:** Да, перезагрузив парсер:
```bash
python main.py
```

Parcel автоматически загружает `channels.json` при старте.

---

## 🔒 Безопасность

### Q: Безопасно ли хранить .env файл на диске?
**A:** Нет, лучше использовать:
- Переменные окружения системы
- Управление секретами (AWS Secrets Manager, HashiCorp Vault)
- Docker secrets

Для локальной разработки .env в `.gitignore` достаточно.

### Q: Что если кто-то получит мой OPENCLAW_GATEWAY_TOKEN?
**A:** Они смогут:
- Добавлять/удалять каналы в парсере
- Включать/отключать обработку

**Решение:**
1. Немедленно измените токен в OpenClaw Gateway
2. Перезагрузите парсер

### Q: Как проверить, что парсер не отправляет данные куда-то ещё?
**A:**
1. Проверьте исходный код (открытый)
2. Используйте network sniffer:
   ```bash
   sudo tcpdump -i any port 3000
   ```

### Q: Может ли аккаунт быть заблокирован?
**A:** Да, если:
- Отправляет слишком много запросов (rate limiting)
- Используется для спама
- Нарушает условия Telegram

**Рекомендации:**
- Используйте отдельный аккаунт для парсера
- Не переборщите с частотой запросов
- Следуйте условиям использования Telegram

---

## 🐛 Отладка и ошибки

### Q: Как увидеть детали ошибки?
**A:** Включите DEBUG логирование:
```bash
LOG_LEVEL=DEBUG python main.py 2>&1 | tee debug.log
```

### Q: Парсер зависает и ничего не логирует
**A:** Возможно зависание при:
- Подключении к Telegram
- Подключении к OpenClaw

**Решение:**
1. Установите timeout:
   ```python
   # Требует модификации кода
   ```

2. Используйте systemd service с `Restart=on-failure`

3. Убедитесь в доступности сетевых ресурсов

### Q: "ModuleNotFoundError: No module named 'pyrogram'"
**A:**
```bash
pip install -r requirements.txt
```

### Q: "ConnectionRefusedError" для OpenClaw Gateway
**A:**
1. Проверьте URL в .env
2. Убедитесь, что OpenClaw запущен
3. Проверьте firewall

```bash
# Проверить доступность
curl -v ws://localhost:3000
nc -zv localhost 3000
```

### Q: "Session expired" при запуске парсера
**A:** Сессия Telegram истекла, переавторизуйтесь:
```bash
rm parser_session.session
python main.py
# Следуйте инструкциям авторизации
```

---

## 📈 Производительность

### Q: Как много каналов может обрабатывать парсер?
**A:** Зависит от:
- Частоты сообщений в каналах
- Объема данных в каждом сообщении
- Характеристик машины

Теоретически - сотни каналов, практически - зависит от вашей инфраструктуры.

### Q: Почему парсер потребляет много памяти?
**A:** Возможные причины:
1. Большой файл лога - проверьте `LOG_FILE`
2. Много каналов в реестре - оптимизируйте
3. Утечка памяти в Pyrogram - обновите библиотеку

**Решение:**
```bash
# Мониторить память
ps aux | grep python

# Анализировать
import tracemalloc
tracemalloc.start()
```

### Q: Как оптимизировать для низкоскоростного интернета?
**A:**
1. Используйте меньше логирования (`LOG_LEVEL=WARNING`)
2. Сжимайте данные перед отправкой
3. Добавьте кэширование

---

## 🌐 Развёртывание

### Q: Можно ли запустить парсер на Raspberry Pi?
**A:** Да, но с ограничениями:
- Медленнее из-за слабого процессора
- Ограниченная память
- Рекомендуется использовать swap

### Q: Как запустить парсер в облаке?
**A:** Примеры:

**AWS EC2:**
```bash
# Создать инстанс
# SSH подключиться
# Установить зависимости
# Запустить парсер в screen/tmux/systemd
```

**Heroku:**
```bash
# Требует Procfile и runtime.txt
# Может быть дорого для постоянного процесса
```

**Docker:**
```bash
docker build -t telegram-parser .
docker run -e TELEGRAM_API_ID=... -e TELEGRAM_API_HASH=... telegram-parser
```

### Q: Как настроить автоперезагрузку при сбое?
**A:** Используйте systemd или supervisor:

**systemd:**
```ini
[Service]
Restart=on-failure
RestartSec=10s
```

**supervisor:**
```ini
[program:telegram-parser]
command=/opt/parser/.venv/bin/python main.py
autostart=true
autorestart=true
```

---

## 📞 Если ничего не помогает

### Чек-лист перед обращением за помощью:

1. ✅ Прочитал README.md и ARCHITECTURE.md
2. ✅ Проверил логи с LOG_LEVEL=DEBUG
3. ✅ Валидировал конфигурацию в .env
4. ✅ Убедился в доступности сетевых ресурсов
5. ✅ Переустановил зависимости: `pip install -r requirements.txt --upgrade`
6. ✅ Попробовал переавторизоваться в Telegram
7. ✅ Проверил версии зависимостей

### Информация для отправки:

- Версия Python: `python --version`
- Логи с DEBUG: `LOG_LEVEL=DEBUG python main.py 2>&1 | head -100`
- Версии зависимостей: `pip list`
- Операционная система: `uname -a`
- Исходящие ошибки (traceback)

### Полезные ссылки:

- [Pyrogram Issues](https://github.com/pyrogram/pyrogram/issues)
- [OpenClaw Docs](https://github.com/openclaw/gateway)
- [Python Docs](https://docs.python.org/)
- [asyncio Docs](https://docs.python.org/3/library/asyncio.html)
